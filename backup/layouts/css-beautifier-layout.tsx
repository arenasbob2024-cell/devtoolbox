import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'CSS Beautifier Online - DevToolBox',
  description: 'Beautify, format and indent CSS code online for free.',
  keywords: ['css beautifier', 'css formatter', 'format css online', 'css pretty print'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
