# DevToolBox 工作记录 — 2026年3月15日

## 本次会话概要

本次会话延续前一轮工作，完成了蓝海关键词分析 + 批量工具创建 + 全部部署上线。

---

## 完成的工作

### 第一批：11 个 DevOps/验证类工具
| 工具 ID | 名称 | 类别 |
|---------|------|------|
| mermaid-editor | Mermaid 图表编辑器 | 编辑器 |
| kubernetes-yaml-validator | K8s YAML 验证器 | 验证器 |
| openapi-validator | OpenAPI 验证器 | 验证器 |
| github-actions-validator | GitHub Actions 验证器 | 验证器 |
| csv-viewer | CSV 查看器 | 查看器 |
| htpasswd-generator | htpasswd 生成器 | 生成器 |
| json-patch-tool | JSON Patch 工具 | 工具 |
| graphql-playground | GraphQL Playground | 编辑器 |
| code-to-image | 代码转图片 | 转换器 |
| docker-compose-validator | Docker Compose 验证器 | 验证器 |
| terraform-formatter | Terraform 格式化器 | 格式化 |

### 第二批 (2a)：5 个高需求蓝海工具
| 工具 ID | 名称 | 目标关键词 |
|---------|------|-----------|
| websocket-tester | WebSocket 测试工具 | websocket tester online |
| totp-generator | TOTP 生成器 | totp generator, 2fa code generator |
| connection-string-builder | 数据库连接字符串生成器 | db connection string builder |
| systemd-unit-validator | Systemd 单元文件验证器 | systemd unit validator (零竞争) |
| api-rate-limit-calculator | API 速率限制计算器 | api rate limit calculator, llm cost |

### 第三批 (2b)：5 个安全/设计/DevOps 工具
| 工具 ID | 名称 | 目标关键词 |
|---------|------|-----------|
| oauth-debugger | OAuth 2.0 调试器 | oauth debugger, oauth2 token inspector |
| dockerfile-linter | Dockerfile 检查器 | dockerfile linter, docker best practices |
| ssl-certificate-decoder | SSL 证书解码器 | ssl certificate decoder, x509 inspector |
| font-pairing-tool | 字体配对工具 | font pairing tool, google fonts combination |
| helm-chart-validator | Helm Chart 验证器 | helm chart validator |

---

## 站点当前状态

- **工具总数**: ~396 个
- **博客文章**: 95+ 篇
- **支持语言**: 15 种 (en, fr, de, it, es, pt, nl, pl, sv, no, zh, ja, ko, id, th)
- **线上地址**: https://viadreams.cc
- **GitHub**: 已同步至 main 分支

---

## Git 提交记录

1. `feat: add 11 new developer tools (mermaid-editor, k8s-validator, openapi-validator, github-actions-validator, csv-viewer, htpasswd-generator, json-patch, graphql-playground, code-to-image, docker-compose-validator, terraform-formatter)`
2. `feat: add 5 new tools (websocket-tester, totp-generator, connection-string-builder, systemd-unit-validator, api-rate-limit-calculator)`
3. `feat: add 5 more tools (oauth-debugger, dockerfile-linter, ssl-certificate-decoder, font-pairing-tool, helm-chart-validator)`

---

## IndexNow 提交

- 共 316 个新 URL 已提交至 IndexNow (Yandex 端点)
- 全部返回 HTTP 200/202 成功

---

## 蓝海关键词分析结论

### 已实施的高优先级机会
- WebSocket 测试 — 高需求，实时应用调试
- TOTP 生成器 — 安全/2FA 测试
- 数据库连接字符串 — 零竞争
- Systemd 验证 — 零 Web 竞争
- API 速率限制/成本计算 — LLM 热潮带动
- OAuth 调试 — 稳定需求
- Dockerfile 检查 — 补全 Docker 工具链
- SSL 证书解码 — 补全安全工具链
- 字体配对 — 前端设计受众
- Helm Chart 验证 — DevOps 蓝海

### 待开发的机会 (下一轮可考虑)
- gRPC 测试工具 (微服务加速)
- Avro Schema 验证器 (数据工程)
- Embedding 可视化 (AI/ML 趋势)
- Bun 兼容性检查器 (新兴运行时)
- SQL 复杂度分析器 (DBA 工具)
- WebAssembly 反汇编器 (WASM 趋势)

---

## 技术备注

- `@/i18n/LangContext` 是正确的 useLang import 路径（非 `@/components/LangContext`）
- `const { dict, lang } = useLang()` — 解构用法
- Turbopack 不支持 JSX 中包含 `${}` 的模板字面量，需提取为变量
- `.next` 缓存偶尔会报 `ENOTEMPTY` 错误，`rm -rf .next` 后重新构建即可
