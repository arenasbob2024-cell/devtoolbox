import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'HTTP Header Checker - DevToolBox',
  description: 'Check HTTP response headers for any URL. Analyze security headers, caching, and more.',
  keywords: ['http header checker', 'check http headers', 'response headers', 'security headers'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
