import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SQL Query Formatter Online - DevToolBox',
  description: 'Format and indent SQL queries online for free. Supports MySQL, PostgreSQL, SQLite.',
  keywords: ['sql formatter', 'sql query formatter', 'format sql online', 'sql beautifier'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
