import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Color Blindness Simulator - DevToolBox',
  description: 'Simulate how colors appear to people with different types of color blindness.',
  keywords: ['color blindness', 'color blindness simulator', 'colorblind', 'deuteranopia', 'protanopia'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
