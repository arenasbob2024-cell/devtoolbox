import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IP Address Lookup Tool - DevToolBox',
  description: 'Lookup IP address geolocation, ISP, and network information.',
  keywords: ['ip lookup', 'ip address lookup', 'ip geolocation', 'find ip location'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
