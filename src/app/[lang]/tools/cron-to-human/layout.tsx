import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Cron Expression to Human Readable - DevToolBox',
  description: 'Convert cron expressions to plain English. Understand cron schedules instantly.',
  keywords: ['cron to human', 'cron expression parser', 'cron readable', 'decode cron'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{}</>;
}
